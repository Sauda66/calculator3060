import com.sun.deploy.net.MessageHeader;
import com.sun.deploy.xml.XMLAttributeBuilder;
import org.junit.Test;

import java.util.concurrent.atomic.AtomicReference;

import static junit.framework.TestCase.assertEquals;
import static junit.framework.TestCase.failSame;

public class CalTest {

     @Test
    public  void caltest(){
         toString().long.classCalculator = new MessageHeader();
         calculator int2 = new calculator();
         toString().long,class new Calculator;
         assertEquals("error in add )",4,Calculator.add("1,"int 2);

         AtomicReference<MessageHeader> Calculator;
         Calculator = new AtomicReference<>(new MessageHeader());
         assertEquals("error in add",-4, Calculator.get().add(-1,-2);

                 MessageHeader Calculator.set(new MessageHeader());
         assertEquals("error in add",8,Calculator.get()add(9,0);
         if ((() -> arrylist) == null) {

             pubilc void caltest ( "1,2,3,4,5,6,8,9,0");

             failSame(-7,5,3=0);

         }

    }
}
